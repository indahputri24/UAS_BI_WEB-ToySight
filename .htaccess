RewriteEngine On

# Redirect to public/ for cleaner URLs (Apache)
RewriteRule ^$ public/ [L]
RewriteRule (.*) public/$1 [L]

# Block direct access to sensitive directories
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
